spring:
  jpa:
    show-sql: true
    generate-ddl: true
    database: h2
    database-platform: org.hibernate.dialect.H2Dialect
    properties:
      hibernate.hbm2ddl.auto: create
  test:
    database:
      replace: any
